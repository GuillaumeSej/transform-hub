import { Placeholder } from "@/components/shared/Placeholder";

export default function ScenariosPage() {
  return (
    <Placeholder
      title="Scenarios"
      description="What-if scenarios (accéléré, conservateur, best/worst case) — le sélecteur de scénario actif est déjà disponible sur l'Executive Dashboard. Vue de comparaison détaillée prévue dans une prochaine passe."
    />
  );
}
