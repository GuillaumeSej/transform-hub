import { Placeholder } from "@/components/shared/Placeholder";

export default function WorkstreamsPage() {
  return (
    <Placeholder
      title="Workstreams"
      description="Vue détaillée par workstream (kanban de pipeline, underperformers, budget vs plan) — synthèse déjà disponible sur l'Executive Dashboard. Module dédié prévu dans une prochaine passe."
    />
  );
}
